# 图案描述

画布大小 512x512 像素，分成 8x8=64 个相同大小的区域，每个区域 64x64 像素。

## 计算公式

a >= 0： `(x ^ y) & a`

a < 0：`(x ^ y) & (~a)`

x，y：小图案的像素坐标，左上角原点

计算结果 0 涂黑，非 0 留白。

在 a>=0 的时候有 64 种图案，对每一种图案，都有一个小于 0 的 a 值画出的黑白互补的图案与之对应，从而一共 2x64=128 种图案，且互补图案的 a 值可通过按位取反得到。

## 单个图案

这一部分以 a>=0 的图案为例说明，如果 a 是负数，黑白互换即可。

a>=0 时，对应的无符号二进制数一共 6 位，可以粗略分为低三位和高三位，低三位决定图案的像素级细节，这里也称作纹理，高三位则决定哪些地方出现纹理，哪些地方留白。

此外如果 a=0，画出的是纯黑。

a 的二进制形式 1 的个数决定了黑白比例，多一个 1，黑少一半。

如果 a 的二进制形式只有一个 1，画出的就是棋盘格图案，最低位对应边长 1，次低位对应边长 2，以此类推，高一位则边长倍之。

棋盘格图案可分成x、y方向上重复出现的单元，一个单元内的分布是：

黑 白
白 黑

或者说：

0 1
1 0

越高位的 1，对应单元尺寸越大，图案分布越宏观/整体；
越低位的 1，对应的就是越细微的部分。

如果有两个 1 或更多，图案的分布可以看成每个 1 对应的不同尺寸棋盘格分布的叠加：有白得白，全黑得黑。

或者说，即从最高位 1 开始，只要遇到一位等于 1，就把已经形成的黑色块（计算结果 0 对应的块），用这个 1 的相应大小的单元替换，白色块不动，直到替换到低位没有 1 为止。

举例，3（011）是怎么得到细线的：

从高位开始，一直到第二位遇到第一个 1，对应黑/白小块边长 2：

黑 黑 白 白
黑 黑 白 白
白 白 黑 黑
白 白 黑 黑

然后，低一位（也是最低位），是 1，把左上角和右下角的 2x2 黑块换成更小的单元：

黑 白 白 白
白 黑 白 白
白 白 黑 白
白 白 白 黑

现在的剩下的 4 个小黑块（像素），恰好排成主对角线，这就是细线的来源。

### 低三位

0（000）：纯黑。
1（001）：中灰，黑白像素交替排列而成。
2（010）：颗粒感的中灰，极细密格子。
3（011）：细线，浅灰，紧凑。
4（100）：细密黑白格子，比2大点，不灰。
5（101）：浅灰小格与白色组合，其中浅灰带有黑白痕迹，黑白的灰。
6（110）：细线，有颗粒感的，中间的白色成线状。
7（111）：细线，没颗粒感的，比3更浅的灰，线与线之间不挤。

### 高三位

0（000）：排满，图案最细密。
1（001）：黑（灰/纹理）、白相间，有一点喘息空间。
2（010）：16x16大格子、白相间，单个图案分成4x4个大格子，有生动感，结合低位能画出层次感。
3（011）：小格子排成3条细线，白多于黑。
4（100）：大块分割，纹理只出现于左上或右下。
5（101）：4的大块分割，加上1的小格子（灰/纹理），有层次感。
6（110）：大格子组成斜线，斜线里面细节丰富。
7（111）：小格子组成斜线，斜线里面细节相对单一。

### 低三位与高三位的结合

只有低三位，则细节/纹理排满整个图案；只有高三位，则全是黑白分明的格子。两者结合，则既有整体又有细节，能画出更丰富有层次感的图形。

像细线两边有小斜线的图案：

11（001011），27（011011），43（101011），59（111011），即除以 16 余 11 的，每一段小斜线只有 3 个像素点，很细密

23（010111），55（110111），小斜线稀疏一些，主对角线两边各 4 段。

47（101111），小斜线更稀疏，主对角线两边，每边只有 2 段。

在不放大黑像素本身的情况下，相当于左移一位之后，取低六位，再把最低位置 1，59（111011）取左上角四分之一得到 55（110111）的结构，重复类似操作得到 47（101111），31（011111）的结构。

黑方格排列成主对角线的图案，内部没细节，按照方格从大到小的顺序排列：

32（100000），48（110000），56（111000），60（111100）， 62（111110），63（111111），其中 63（111111）就是一条细线

如果二进制表示有周期，或者近似周期，那么对应的图案层次感丰富，带有一定的分形特点，例如 21（010101），42（101010），有 3 个层次。

## 多个图案的结合

不同图案排列组合，能画出更丰富的图案。例如（仅供参考，实际上可能的放置方式很多，而且不要求非要采取某一种或几种放置方式）：

把相同或相近的图案相邻放置：

高三位相同，低三位相同，灰度相同等。

把按照规律变化的图案相邻放置，形成渐变效果：

高三位，或者低三位，递增或者递减；灰度递增或递减。

斜线交叉放置图案，或者按照分形结构放图案。

也可以把画布分成不同部分来放图案，或者不同类型/结构的图案占据不同的位置。

利用黑白互补画更多图案。

注意：a 取 2 或 4 的图案如果放太多，看起来不好看，甚至可能伤眼睛。
