# 待修问题清单与后续方向（2026-08-25）

> 仅记录，不修。用户 2026-08-25 实测（跑 `python agent.py` 验证 SSE 实时推送）后汇总；逐条新开对话修。

## 一、已知问题

### 1. `--detailed` 语义混淆：没加 `--detailed`，UI 照样显示思考过程

- 现象：测试 SSE 时没加 `--detailed`，网页「AI 决策过程」卡照样实时显示思考。
- 根因分析：`--detailed` 只控制**日志文件**是否记录模型思考与工具调用原文（`logger.py` 的 `log_model_response`/`log_tool_call`），与 UI 显示无关；UI 思考来自 SSE `reasoning` 事件（`write_reasoning_live`），两者是独立链路。用户预期 `--detailed` 是"显示思考"的总开关，实际不是，语义易误导。
- 待修方向：改名/文档澄清（`--detailed` 明确为"详细日志"），或加独立开关控制 UI 思考显示；先定语义再动。

### 2. 最后几个格子显示不出来

- 现象（2026-08-24 真实运行已记录，本次复述）：运行完成时「AI 决策过程」卡最后几个 set_region 未显示，但终端已输出。
- 疑点：最后一轮点击后运行立即结束（coverage==63 判定），UI 最后一次轮询未抓到最终 state.json，或最后几格点击与结束快照时序竞态。SSE 化后是否仍复现待测（理论上 state 事件即时推，可能已修，待验证）。
- 详见 context-log/2026-08-24_实时卡布局方案对比与定稿.md「待修 bug 记录」第 1 条。

### 3. 智能跟随滚动：翻历史后滑回底部很难恢复跟随

- 现象：时间线卡向上翻看历史后，再滑到底部不一定能成功恢复自动跟随。
- 分析：恢复条件是"距底 < 40px"（`scrollHeight - scrollTop - clientHeight < 40`），需真正贴底才恢复；内容持续追加使 scrollHeight 增长，用户滑到底部时若差一点不满足阈值就不恢复，体验上"很难成功"。
- 待修方向：加大恢复阈值 / 增加「回到底部」按钮 / 改为检测 scroll 方向；先定交互再动。
- ✅ 已修复（2026-08-25，本次对话）：用户定「按钮兜底 + 方向感知自动恢复」。向上滚且距底 ≥40px 暂停；向下滚且距底 <150px 恢复；暂停时右下角浮现「↓ 回到底部」按钮。浏览器实测（模拟 SSE 流 + 移动靶）：暂停/恢复/按钮全通过。详见 context-log/2026-08-25_滚动跟随修复.md。

### 4. AI 重复点击已点击过的区域，导致提前终止

- 现象：用户测 `--detailed` 语义的那次运行（日志名 `xor_agent_20260825_215015.log`，**该文件已不在磁盘**：`*.log` 被 .gitignore 忽略且测试后未保留，复现时需重新跑或向用户要日志）AI 重复点击已点击过的区域，消耗点击次数提前触达终止条件。
- 疑点：用户怀疑该轮 AI 改了设计、换了种方案（需看日志确认）；系统提示词已要求"每个非种子区域都要被点击设定过"，但未禁止重复点击；`coverage()` 按去重计数，重复点击不计入覆盖但计入 `click_count`。
- 待修方向：先解析日志确认真实行为，再定是提示词约束还是计数器语义（重复点击是否应计入）。

## 二、后续方向

### 5. 不依赖图片的加载方案

- 用户问题：可视化只需要参数 grid（64 个值）就够了，为何依赖 512×512 PNG 加载？
- 事实：前端已有 heatmap 用 canvas 按 grid 绘制（64 格色块）；主画布 `<img>` 依赖 `/output/step_NNN.png`。`(dx^dy) & a` 渲染逻辑在 numpy 实现，JS 复刻可完全免 PNG。
- 用户约定：**防范，最简方案、跑得快的方案优先，一开始就先摸清楚**（先摸清 JS 复刻渲染的可行性与成本，再定是否实施）。

### 6. 模拟 Agent 测试脚本

- 用户问题：是否写一个模拟 Agent 测试，用原始真实日志数据（如 xor_agent_20260823_214323.log / 215251.log）尽可能模拟真实 Agent 完整决策过程？
- 用途：浏览器实测 UI（思考流式、点击轨迹、结束状态）不消耗 API token；2026-08-24 的 probe_sim_live.py 只模拟了思考写入，本次要求更完整（思考 + set_region/view_region/evaluate 工具调用 + 快照 + 结束）。
- ✅ 已实现（2026-08-26）：`tests/sim_agent.py`（解析 example_data.txt → 按时间线驱动 `write_reasoning_live` 思考流式 + `tools.execute_tool` 工具 + `agent.snap` 快照 + 结束状态/统计，含数据完整性校验 63/63）。回放速度 = 日志时间戳倒推：事件间隔按 `[时间戳]` 差值推进，每轮思考流式在「本轮请求耗时」内均匀流出（speed=1 即原始运行真实节奏，example_data.txt 首尾 201s；`--speed N` 倍速）。推送走真实 SSE 链路（`write_reasoning_live`/`snap` 内部 `_sse_broadcast`，浏览器 EventSource 连 /events，实测 6s 收到 306 帧 reasoning 事件）。流式帧率 ~20 帧/s（0.05s/批，0.3s/批会一卡一卡），文本流完后剩余时间继续占用请求时长（模型沉默思考）。运行：`python tests/sim_agent.py`（默认 8765 端口自动打开浏览器，`--no-open --exit` 无人值守，`--dry-run` 只解析）。实测：回放完整 8 轮 / 72 工具 / 63 点击，覆盖 63/63 无未设定；思考流式总时长 197.4s ≈ 真实跨度 201s。README 结构清单已同步 tests/ 条目。踩坑：模型输出含 U+2212（−）在 GBK 控制台 print 崩 → 脚本内 `sys.stdout.reconfigure(encoding="utf-8")`。
- ✅ 存放方案已定（2026-08-26，用户定「新建 tests/ 目录常驻」）：与 selftest.py 同类的可复用测试工具，新建 `tests/` 目录 git 跟踪常驻（不按一次性探针用完即删）；真正写代码时同步 README 结构清单。
- ✅ 样例日志入库决策已定（2026-08-26，用户定「b：改名 .txt」）：样例日志复制为 `.txt` 放 tests/，`.gitignore` 零改动（曾排除 `git add -f`：后续修改不显示状态；对比过「保留 .log + tests/sample_logs/ 子目录白名单」：白名单规则需维护）。模拟 Agent 按内容格式解析，与后缀无关。
- ✅ 具体样例已定（2026-08-26）：选 `xor_agent_20260826_003643.log`（最近一次完整运行，SSE 化后格式与当前代码匹配），改名为 `example_data.txt` 放 tests/。数据完整：8 轮循环 / 72 次工具调用 / 63 次成功点击，含思考流式 + set_region/view_region/evaluate 三种工具 + 结束统计（63/63）。

## 三、滚动跟随修复后新发现（2026-08-26记录）

### 7. 方向键滚动时间线：按 ↑ 不立刻翻上方文本（第 3 条修复的残余）

- 现象：想用键盘方向键看上方历史，按 ↑ 不会马上滚动时间线内容。
- 分析：`#timeline` 是普通 div，默认不可聚焦（无 `tabindex`），键盘方向键不作用于它；且刚点过「回到底部」按钮时焦点留在按钮上，方向键更不会滚时间线。上一轮修复只覆盖了鼠标滚轮通道，键盘通道未处理。
- 待修方向：`#timeline` 加 `tabindex` 使其可聚焦（点击/滚动后聚焦），或容器监听 keydown 自行滚动；按钮点击后把焦点还给时间线。先定交互再动。

### 8. 「回到底部」按钮绿底与思考绿字颜色接近，不易区分

- 现象：按钮深绿底（`#2a7`）与思考文字浅绿（`.why` `#8c8`）同为绿色系，扫一眼难以区分按钮和文本。
- 分析：按钮背景 `#2a7`（rgb 34,170,119）与思考文字 `#8c8` 色相相近；页面已有蓝（组头 `#9cf`）、橙（状态/格值 `#fc9`）两个强调色可用。
- 待修方向：按钮换蓝 `#9cf`（与组头一致）或橙 `#fc9`（与状态一致），或中性灰底白字，顺带调整 hover 态。先定视觉再动。
