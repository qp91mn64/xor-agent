# 设计文档

> 2026-08-04 双钻讨论收敛。原始问题见 [README.md](README.md)。

## 一、问题定义

画布 512×512，8×8=64 个区域，每区一个整数参数 `a ∈ [-64, 63]`（128 种取值），图案是 `(dx^dy) & a` 的纯函数，负值只换黑白。参数空间 128^64 ≈ 10^134，暴力搜索不可能。

**核心问题：** 让一个纯文本 LLM 为这个纯函数图像选择参数。难点不在"画"（画是确定的纯函数），而在**评价闭环**——AI 看不见图，评价信号断链，它不知道下一步往哪改。

讨论中澄清的三个关键认识：
1. "模拟点击"是伪命题：点击是人的接口（点→看→判断→再点），AI 的接口是文字/JSON。真正的问题是 AI 能否复现"看→想→改"这个评价循环。
2. 评价信号不一定需要视觉：这个图是纯函数，代码可以直接算指标（黑白比例等），"计算"可代替"看"。
3. 图案可以文字精确描述（位平面叠加，见技术设计），AI 不用看图也能推理。

拆成三个子问题：
1. **描述**：图案语义文字化。
2. **评价**：v1 用代码指标代替视觉。
3. **闭环**：function-calling 逐格点击 + evaluate 观察。

## 二、方案决策

| 决策点 | 选择 | 理由 |
|---|---|---|
| 交互粒度 | 逐格点击（set_region/evaluate） | 最忠实于"模拟点击"；与 test-agent 的 move/view 同构，主循环可整体复用 |
| 评价闭环 | 代码指标迭代（v1 单指标） | 纯文本闭环，无视觉依赖，最贴 agent 本质 |
| 指标配置 | 黑白平衡度 1 个 | 先跑通闭环，指标库是后续扩展点 |
| 种子约束 | 保留（用户给 1 个初始值，AI 填其余） | 忠于原始问题 |
| 决策可视化 | 实时网页（stdlib http.server + 轮询，无框架无 Flask） | 满足"顺便可视化决策过程"，零新框架 |
| 技术栈 | Python | 复用 test-agent 的 agent.py/tools.py/logger.py |
| 运行代码位置 | 根目录（test-agent-src 仅作上游参考） | 导入/运行简单，来源边界清晰，依赖记账不混淆 |

## 三、技术设计

### 工具协议

| 工具 | 参数 | 作用 | 对应 maze |
|---|---|---|---|
| `set_region` | index(0-63), value(-64..63) | 点一个区域，设定其参数值 | move |
| `view_region` | index | 查看某区域当前值和图案描述 | view(局部) |
| `evaluate` | 无 | 渲染整幅并返回指标文本 | view(整体) |

主循环：AI 调用工具 → 执行 → 结果回填 → 循环，直到 AI 不再调用工具或触达终止条件。每次工具调用后写一份 state.json + PNG 快照，供网页实时刷新。

### 图案语义字典

- 每个区域值 `a` 决定 `(dx^dy) & a` 的图案；**a 的二进制第 k 位 = 叠加一种块大小 2^k 的棋盘格**。
- a=0 纯黑；a=1 是 1px 细棋盘格；a=2 是 2×2 方块棋盘；a=4 是 4×4 方块棋盘；多位置位=各尺度叠加（如 a=5 = 1px ∪ 4×4）；a=63 最密。
- 负值 = 反色版（黑白互换）；a=-1 全白。

### 指标 v1

`黑白平衡度 = 1 - |黑像素占比 - 0.5| × 2`（渲染后数像素，精确）。

**已知坑（诚实记录）**：单指标存在作弊解——全填 ±63（满棋盘格）即满分。v1 目的不是求好作品，而是把"闭环+可视化"跑通。后续扩展：图案多样性、网格对称度、邻域渐变度、纹理密度，再谈审美。

### 循环与终止

复用 test-agent 的终止条件模式，参数可调：
- 最多 N 次循环（一次循环 = 一次 API 请求，默认 30）
- 最多 M 次 set_region（默认 64，即每格一次点击机会）
- AI 连续输出文字不再调用工具即视为结束

成功判定不看模型输出，看最终画布指标 + 是否使用了种子以外的全部区域（状态为准）。

## 四、工程与扩展

### 项目结构与复用清单

```
test-agent-2/
├── agent.py        # 从 test-agent-src 复制改造：主循环（换 SYSTEM_PROMPT、工具、终止条件、成功判定）
├── xor_world.py    # 新写：网格状态 + 图案语义 + 渲染(numpy) + 指标 + 种子初始化
├── tools.py        # 新写：set_region / view_region / evaluate 的 schema 与执行
├── logger.py       # 从 test-agent-src 原样复制
├── web/index.html  # 新写：实时决策可视化（原生 JS，轮询 state.json）
├── requirements.txt
├── env.example     # 从 test-agent 复用
└── output/         # 每次工具调用后的 state.json + png + 日志（可回放）
```

复用清单（test-agent-src 提供原样参考，运行代码在根目录；**发布时删除该目录**，改用上游链接 [minimal-agent](https://github.com/qp91mn64/minimal-agent)）：
- `agent.py` 主循环骨架：API 调用、assistant_message（reasoning_content 回传防御）、工具回填、终止条件、日志。
- `logger.py`：原样。
- `.env` / get_api_key / env.example 模式：原样。
- `tools.py` 结构：schema + execute_tool 分派（工具本体替换）。
- 许可证沿用 test-agent 的 MIT。
- `maze.py` 用不上（被 xor_world.py 替代），留在 src 仅作世界模块参考。

### 实时可视化

- 每步（每次工具调用后）agent 写入 `output/state.json` + 最新 PNG。
- `web/index.html` 每秒轮询 state.json，原生 JS 渲染：当前大图、64 区参数热力图、AI 点击轨迹时间线（哪格被点成什么值）、AI 思考内容（详细日志的 reasoning_content）、指标演化。
- 启动方式：`python agent.py` 自动起 stdlib HTTP 服务并打印 URL。

### 依赖与许可证

| 包 | 许可证 | 用途 |
|---|---|---|
| openai | Apache-2.0 | DeepSeek API 客户端 |
| python-dotenv | BSD-3-Clause | 读取 .env |
| numpy | BSD-3-Clause | 像素级渲染计算 |
| pillow | HPND | 输出 PNG |

全部宽松许可证，无 GPL 依赖。DeepSeek API 为服务调用，不构成代码依赖。原始 HTML 的 p5.js 为 LGPL-2.1，新实现用 numpy 复刻渲染逻辑，不依赖它。

### 基线对照与后续扩展

**基线对照**（回答"AI 是否胜过运气"）：内置 random 策略，随机 set_region 同样的点击次数，跑同样的指标。比较最终指标，得出"纯文本 AI 在这个空间里是否超越了随机"——这是"探索 AI 创造力边界"的可量化答案。

**后续扩展点**：
1. 指标库扩展：图案多样性、网格对称度、邻域渐变度、纹理密度 + 复合分。
2. 视觉 API 评价（AI 生成 → 渲染 → 视觉模型看图 → 文字评价 → 再改）。
3. 批量提交模式（set_regions 一次改多个区域）。
