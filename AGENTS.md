# AGENTS.md

## 项目管理规则

- 不同项目、不同事情，分开存档：日志、测试结果、记忆记录等按项目（及用途）分目录存放，不混在一起。

## 操作原则

- **不得泄露密钥**，也不得尝试获取密钥。
- **写进文件的才算归档**，对话记录不算。
- 修改一处之后检查是否存在不一致：代码内部，文档内部，以及代码与文档之间。
- **同一文件的多处修改要串行执行，不要并行**：并行编辑同一文件同一行时，后写入的结果会覆盖前面的修改。改完用 Grep/Read 复查确认。

## 测试规则

- 测试结果保存为独立文件。
- 日志名称加上时间戳。
- 代码改动后，先跑不依赖外部服务的自测（如迷宫逻辑可离线验证），通过后再交付

## 文档原则

- 三级及细分目录，相同层次小标题不超过4个。
- 设计文档要能看出唯一技术实现（不并列替代方案，避免歧义）。
- 文档产出前自检：命名查惯例、先整体后细节、约束唯一、与代码对照。

## 交互原则

- 禁止回答“你说得对”。
- 实事求是，拿不准或不知道，就提问，或者如实承认。
- 回答有依据，并尽可能找到原始出处。。

## 项目特定约定（DeepSeek API）

- **工具调用轮次回传 reasoning_content**：官方文档要求，思考模式下进行了工具调用的轮次，后续请求必须完整回传 `reasoning_content`，否则 API 返回 400（见 [DeepSeek thinking_mode 文档](https://api-docs.deepseek.com/zh-cn/guides/thinking_mode)）。尽管曾经在不同项目里面实测过 deepseek-v4-flash 0731 没触发 400，保留回传以防万一。
- **排查 agent 行为**：用 `python agent.py --detailed`（或环境变量 `DETAILED_LOG=1`）记录模型每轮的思考与输出；`--help` 可看用法。
- **依赖与许可证**：升级依赖后按 `DEPENDENCIES.md` 重新核对许可证；新增依赖先加进 `requirements.txt` 再使用。
- 详细排查记录见 `context-log/`，一个主题一个文件。
