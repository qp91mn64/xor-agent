# AGENTS.md

## 操作原则

- **不得泄露密钥**，也不得尝试获取密钥。
- **及时归档防丢失**：经验，方法，决策过程等，使用 context-log skill 归档。
- **同一文件，串行修改**：一处改完才能修改下一处。改完用 Grep/Read 复查确认。
- 修完之后，进行一致性检查：代码内部，文档内部，以及代码与文档之间。

## 执行规则

- **实事求是**：基于实际情况，而不是默认假设。把所有默认假设看成错的（错误假设示例：实际API速度是60字符/秒）。不懂就提问，或者测试，而不是猜测。实际情况包括但不限于：真实的需求，人（以及AI）本来的行为方式，实际的操作，文件的实际内容，程序的实际执行结果，实际限制因素。
- 拿到需求，或者修bug之前，先溯因：找根本原因，多问是什么和为什么，确定问题。不准拿到问题就开工。
- 找出原因之后，确定方案：头脑风暴可能方案，只要有至少两种，而且现有文件不足以确定，就提问。不得自作聪明地在无依据的情况下排除方案。

## 设计原则

- 意符清晰可感知：不同类型的UI控件，避免使用难以区分的颜色/样式
- 修改设计时，不能破坏其余部分的原有交互
这个部分，参考了诺曼《设计心理学1：日常的设计》。

## 测试规则

- **遇事不决就实际测试**，用测试结果说话。
- 测试结果保存为独立文件。
- 日志名称加上时间戳。
- 代码改动后，先跑自测，通过后再交付

## 文档原则

- 不同项目、不同事情，分开存档：日志、测试结果、存档记录等按项目（及用途）分目录存放，不混在一起。
- 三级及细分目录，相同层次小标题不超过4个。
- 设计文档要能看出唯一技术实现（不并列替代方案，避免歧义）。
- 文档产出前自检：命名查惯例、先整体后细节、约束唯一、与代码对照。

## 交互原则

- 禁止回答“你说得对”。
- 拿不准或不知道，就提问，或者如实承认。
- 回答有依据，并尽可能找到原始出处。
- 回答简洁一点。

## 其他

- 工具调用轮次回传 reasoning_content：按 DeepSeek 官方文档，思考模式+工具调用，后续请求必须完整回传 `reasoning_content`，否则 API 返回 400（详见 https://api-docs.deepseek.com/zh-cn/guides/thinking_mode）。尽管曾测过 deepseek-v4-flash 0731 没触发 400，保留回传以防万一。
- 排查 agent 行为：运行日志（`xor_agent_*.log`）默认记录模型思考、工具调用与输出。
- 依赖管理：升级依赖后，检查 `DEPENDENCIES.md` ，重新核对许可证；新增依赖先加进 `requirements.txt` 再使用。
